CC = g++

	# compiler flags:

 	CFLAGS  = -lnfc -std=c++11 -pthread

  # the build target executable:
	TARGET = myprog

	all: $(TARGET)

	$(TARGET): $(TARGET).c
		$(CC) $(CFLAGS) -o $(TARGET) actionrec.cc imu.o

	clean:
		$(RM) $(TARGET)
